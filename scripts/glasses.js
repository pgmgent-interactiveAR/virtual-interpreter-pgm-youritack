const FreeGlasses = () => {
  const glasses = document.querySelector('#glassesmodel');
  const subtitles = document.querySelector('.subtitles')
    glasses.setAttribute('visible',true);
    subtitles.textContent ='webspeech kit is not supported for this browser or something went wrong. \r\n so have some glasses!';
  };
  
  export default FreeGlasses;
  
  